import { FieldServiceRegex } from './field-service-regex';

describe('FieldServiceRegex', () => {
  it('should create an instance', () => {
    expect(new FieldServiceRegex()).toBeTruthy();
  });
});
